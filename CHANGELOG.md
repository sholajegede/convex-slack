# Changelog

## 0.0.3

### Patch Changes

- Fix the Convex directory badge image URL, which still pointed at `badge/sholajegede/convex-slack` after the link URL was corrected in a previous release; both now use the plain `badge/convex-slack` path.

## 0.0.2

### Patch Changes

- Document that getStats scans all 7 tables to compute counts, which is O(n) in total rows rather than O(1)

## 0.0.1

Initial release.
